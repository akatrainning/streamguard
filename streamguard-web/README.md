# StreamGuard Web

React + Vite frontend for the StreamGuard dashboard.

```powershell
npm install
npm run dev
```

The development server proxies API and WebSocket traffic to
`http://localhost:8011`.
